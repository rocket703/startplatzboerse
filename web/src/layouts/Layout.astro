---
import Header from '../components/Header.astro';
import SpeedInsights from '@vercel/speed-insights/astro';
import '../styles/global.css';

const { 
  title, 
  description = "Die Community für Startplätze. Kaufe und verkaufe Tickets für Marathon, Triathlon, Radrennen und Hyrox sicher und unkompliziert.",
  image = "/og-default.png", 
  canonicalURL: canonicalURLProp,
  ogType = "website" // NEU: Standard ist "website"
} = Astro.props;

/** Immer Produktions-Origin — Astro.url ist unter Vercel SSR oft https://localhost */
const siteOrigin = Astro.site ?? new URL('https://startplatzboerse.com');
const pageURL = new URL(Astro.url.pathname, siteOrigin).href;
const canonicalURL = canonicalURLProp ?? pageURL;
const absoluteImage = new URL(image, siteOrigin).href;
---

<!DOCTYPE html>
<html lang="de">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <meta name="generator" content={Astro.generator} />

        <title>{title} | Startplatzbörse</title>
        <meta name="title" content={`${title} | Startplatzbörse`} />
        <meta name="description" content={description} />
        <link rel="canonical" href={canonicalURL} />

        <meta property="og:type" content={ogType} />
        <meta property="og:url" content={canonicalURL} />
        <meta property="og:title" content={`${title} | Startplatzbörse`} />
        <meta property="og:description" content={description} />
        <meta property="og:image" content={absoluteImage} />

        <meta property="twitter:card" content="summary_large_image" />
        <meta property="twitter:url" content={canonicalURL} />
        <meta property="twitter:title" content={`${title} | Startplatzbörse`} />
        <meta property="twitter:description" content={description} />
        <meta property="twitter:image" content={absoluteImage} />

        <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
        <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
        <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
        <link rel="manifest" href="/site.webmanifest" />
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />

        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;900&display=swap" rel="stylesheet">

        {/* Verhindert DE-Aufflackern: Locale aus Storage/Browser vor dem ersten Paint setzen */}
        <script is:inline>
            (function () {
                var STORAGE_KEY = 'spb_locale';
                var stored = localStorage.getItem(STORAGE_KEY);
                var locale = null;
                if (stored === 'de' || stored === 'en' || stored === 'it') {
                    locale = stored;
                } else {
                    var langs =
                        navigator.languages && navigator.languages.length
                            ? navigator.languages
                            : [navigator.language || ''];
                    for (var i = 0; i < langs.length; i++) {
                        var primary = String(langs[i] || '').toLowerCase().split('-')[0];
                        if (primary === 'en' || primary === 'it' || primary === 'de') {
                            locale = primary;
                            break;
                        }
                    }
                }
                locale = locale || 'de';
                document.documentElement.lang = locale;
                document.documentElement.dataset.locale = locale;
                if (locale !== 'de') {
                    document.documentElement.classList.add('i18n-blocking');
                }
            })();
        </script>
        <style is:global>
            html.i18n-blocking [data-i18n],
            html.i18n-blocking [data-i18n-html],
            html.i18n-blocking [data-i18n-placeholder] {
                visibility: hidden;
            }
        </style>
    </head>
    <body>
        <Header />
        <slot />
        <SpeedInsights />
        <script>
            import { initI18n } from '../i18n';
            initI18n();
        </script>
    </body>
</html>
